namespace HarmonyInTheHome.Services {

    //export class CategoryService {
    //    private catSvc;

    //    public listCats() {
    //        return this.catSvc.query();
    //    }

    //    constructor(private $http: ng.IHttpService, private $state: ng.ui.IStateService) {
    //        this.$http.get("/api/categories").then((response) => {
    //            if (response.status == 200) {
    //                this.catSvc = response.data;
    //            } else {
    //                console.error(`There was a problem connecting to the API, Please contact your system Administrator.`);
    //            }
    //        });
    //    }
    //    angular.module('HarmonyInTheHome').service('categoryService', CategoryService);

    //    export class MyService {

    //    }
    //    angular.module('HarmonyInTheHome').service('myService', MyService);
    }
